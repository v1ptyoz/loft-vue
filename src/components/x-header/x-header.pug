header.header
    div.container
        slot
            nav.header__topline
                a(href="/")
                    Logo
                x-nav(:current-user="currentUser")
        slot
            div.stories
                Stories(:user-stories="userStories")
