header.header
    div.container
        slot
            nav.header__topline
                a(href="/")
                    icons(name="logo")
                x-nav(:current-user="currentUser")
        slot
            div.stories
                Stories(:user-stories="userStories")
